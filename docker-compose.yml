version: "3.9"

services:
  casamento:
    build: .
    container_name: casamento_site
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - db_data:/app/prisma

volumes:
  db_data: